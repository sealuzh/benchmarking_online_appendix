---
driver:
  name: vagrant
  network:
    - ["private_network", {ip: "192.168.34.51"}]
  synced_folders:
  #  - ["data/%{instance_name}", "/shared"]
  #username: ubuntu
   - ["synced", "/synced"]


provisioner:
  name: chef_zero

# Uncomment the following verifier to leverage Inspec instead of Busser (the
# default verifier)
# verifier:
#   name: inspec

platforms:
  - name: ubuntu-14.04
    driver:
      customize:
        memory: 1024

suites:
  - name: default
    run_list:
      - recipe[jm-acmeair-default-assets::default]
    attributes:
